# This project supports both npm and yarn
# If using npm, it will create package-lock.json
# If using yarn, it will use yarn.lock
# Both lock files can coexist in the repository

# Resolve Sanity peer dependency conflicts (sanity vs next-sanity @sanity/ui versions)
legacy-peer-deps=true
